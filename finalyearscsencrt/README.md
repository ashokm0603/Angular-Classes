## Mouse Events :

1. Mouse over
2. mouse in
3. mouse out
4. mouse enter
5. mouse move
6. mouse leave

## Bootstrap

    ->Bootstrap is frontEnd frameWord used to design UI (User interface)
    ->Bootstrap contains inbuilt HTML , CSS and JS code
    ->To setup bootstrap to angular we need to install
            bootstrap by giving command  :

```bash
    npm i bootstrap
```

    ->Once bootstrap is install after that we need to or configure to angular.json (styles)
    -> "./node_modules/bootstrap/dist/css/bootstrap.min.css"
